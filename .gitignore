# Dependencias y artefactos de build
node_modules/
build/
dist/
**/*.tsbuildinfo

# Cobertura y reportes pesados
coverage/
lighthouse-*.json
.nyc_output/

# Secretos — NUNCA commitear .env real
.env*
!.env.example

# Datos de ejecución (se montan como volumen / se regeneran)
uploads/
server/uploads/

# Dump local de la BD (contiene datos reales del negocio; el esquema se aplica vía migraciones)
maison_rosas_database.sql

# Sistema operativo / editor
.DS_Store
Thumbs.db
Desktop.ini
nul
*.log
*.swp
.idea/
.vscode/
*.tmp

# ZCode local
.zcode/

# Freebuff AI (dev tooling - solo local)
.freebuff/

# Vercel
.vercel
.vercelenv

# DuckDNS (contiene token real)
duckdns/duck.sh
duckdns/duckdns.service
duckdns/duckdns.timer
duckdns/auth.sh
duckdns/cleanup.sh

# Skills de IA (dev tooling - solo local)
.agents/
skills-lock.json

# Archivos locales de producción
informe-produccion.txt
List

# Scripts de despliegue local (contienen rutas del servidor)
deploy-produccion.sh

# ⚠️ Contiene credenciales reales de la BD de Railway — NO subir a GitHub
.vercel.env.example

# Archivo basura generado por redirecciones de Windows (nunca commitear)
_nul

.claude/
agent/

# Logs locales de desarrollo
logs/
