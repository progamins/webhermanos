# Dependencias y artefactos de build
node_modules/
build/
dist/
**/*.tsbuildinfo

# Cobertura y reportes pesados
coverage/
lighthouse-*.json
.nyc_output/

# Secretos — NUNCA commitear .env real
.env*
!.env.example

# Datos de ejecución (se montan como volumen / se regeneran)
uploads/
server/uploads/

# Sistema operativo / editor
.DS_Store
Thumbs.db
Desktop.ini
nul
*.log
*.swp
.idea/
.vscode/
*.tmp

# ZCode local
.zcode/

# DuckDNS (contiene token real)
duckdns/duck.sh
duckdns/duckdns.service
duckdns/duckdns.timer
duckdns/auth.sh
duckdns/cleanup.sh

# ngrok — config local + tokens, NO commitear
.ngrok/
ngrok.yml

# Túneles locales (contienen ngrok auth token y admin path real)
tunnel.ps1
tunnel.sh

# Skills de IA (dev tooling - solo local)
.agents/
skills-lock.json

# Archivos locales de producción
informe-produccion.txt
List

# Scripts de despliegue local (contienen rutas del servidor)
deploy-produccion.sh
